# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.js >> User cannot login with invalid credentials
- Location: tests\login.spec.js:5:1

# Error details

```
Error: page.goto: NS_ERROR_FILE_UNRECOGNIZED_PATH
Call log:
  - navigating to "file:///var/jenkins_home/workspace/playwright-demo/static/login.html", waiting until "load"

```

# Test source

```ts
  1  | // pages/login.page.js
  2  | const { expect } = require('@playwright/test');
  3  | 
  4  | class LoginPage {
  5  |   constructor(page) {
  6  |     this.page = page;
  7  |     this.usernameInput = page.locator('#username');
  8  |     this.passwordInput = page.locator('#password');
  9  |     this.loginButton = page.locator('button[type="submit"]');
  10 |     this.errorMessage = page.locator('.error-message');
  11 |   }
  12 | 
  13 |   async goto() {
> 14 |     await this.page.goto('file:///var/jenkins_home/workspace/playwright-demo/static/login.html');
     |                     ^ Error: page.goto: NS_ERROR_FILE_UNRECOGNIZED_PATH
  15 |   }
  16 | 
  17 |   async login(username, password) {
  18 |     await this.usernameInput.fill(username);
  19 |     await this.passwordInput.fill(password);
  20 |     await this.loginButton.click();
  21 |   }
  22 | 
  23 |   async assertLoginFailed() {
  24 |     await expect(this.errorMessage).toBeVisible();
  25 |   }
  26 | }
  27 | 
  28 | module.exports = { LoginPage };
```